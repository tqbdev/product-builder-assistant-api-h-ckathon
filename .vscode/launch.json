{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Debug application",
      "request": "launch",
      "command": "pnpm run start:debug",
      "cwd": "${workspaceFolder}/"
    }
  ]
}
