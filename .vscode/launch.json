{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start:debug",
            "request": "launch",
            "command": "pnpm run start:debug",
            "cwd": "${workspaceFolder}/"
        }
    ]
}
